# Fabric benchmark results

This directory stores captured result artefacts produced by the Fabric benchmark workflows.

## Contents
- `caliper/`: Caliper result documents and exports.
- `nodejs/`: standalone Node.js benchmark result documents.

## Notes
- These files are derived artefacts. Keep only the result sets that are actually cited or needed for reproducibility.
# Fabric network configurations

This directory contains the network configuration files for both consensus variants used in the project.

## Contents
- `raft/`: Raft-based benchmark network configuration.
- `smartbft/`: SmartBFT-based benchmark network configuration.

## Notes
- Keep connection profiles, Compose files, and benchmark profiles synchronized within each variant.
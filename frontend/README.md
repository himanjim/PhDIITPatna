# Frontend module wrapper

This directory contains the UI demonstration application without imposing any additional structure around it.

## Contents
- `evote-ui-demo_v3/`: the current frontend demo.

## Notes
- Keep `evote-ui-demo_v3` structurally unchanged unless there is a compelling reason to fork or replace it.
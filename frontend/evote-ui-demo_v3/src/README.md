# Frontend source

This directory contains the Preact application source for the UI demo.

## Contents
- Application entry points (`main.tsx`, `app.tsx`, `router.tsx`).
- Shared state, styles, and i18n support.
- Feature directories for components, pages, services, and utilities.

## Notes
- The current structure is appropriate for a small demo application and does not need reorganization.
# Shared UI components

This directory contains reusable UI components used across the frontend demo.

## Contents
- Header and layout elements.
- Camera capture, QR receipt rendering, and QR scanning components.

## Notes
- Keep filenames stable unless there is a strong need to convert the component naming convention